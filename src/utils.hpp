#pragma once

#include <cstddef>
#include <cstdint>
using u8 = uint8_t;
using u16 = uint16_t;
using u32 = uint32_t;
using u64 = uint64_t;
using usize = size_t;
const u64 U64MAX = UINT64_MAX;
const u32 U32MAX = UINT32_MAX;
const u16 U16MAX = UINT16_MAX;